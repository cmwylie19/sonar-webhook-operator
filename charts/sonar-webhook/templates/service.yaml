apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonar-webhook.fullname" . }}
  labels:
    {{- include "sonar-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonar-webhook.selectorLabels" . | nindent 4 }}
